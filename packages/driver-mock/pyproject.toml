[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "workspace-fabric-driver-mock"
version = "1.0.0"
description = "Workspace Fabric Mock Drivers - In-memory test drivers"
readme = "README.md"
requires-python = ">=3.14"
license = "Apache-2.0"
authors = [{ name = "Brad Richins" }]
dependencies = [
    "workspace-fabric-driver-api>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
]

[project.entry-points."workspace_fabric.drivers"]
mock_video_matrix = "workspace_fabric_driver_mock.plugin:get_mock_video_plugin_descriptor"
mock_usb_matrix = "workspace_fabric_driver_mock.plugin:get_mock_usb_plugin_descriptor"

[tool.black]
line-length = 100
target-version = ["py314"]

[tool.pytest.ini_options]
pythonpath = ["src", "../driver-api/src", "../core/src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["src", "tests"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["workspace_fabric_driver_mock*"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"
